# TEMP — 提示性与讨论性记录（非设计文档）

> 本文件收录 `OARWM_ResWorld.md` 中**移除的提示性/论证性/讨论性**内容：论文答辩词、limitation 讨论与解决状态、实现边界澄清、设计权衡记录。设计文档只保留"符合代码最终设计"的确定性内容；本文件供写作论文与答疑时查阅。
> 相关：设计文档 `OARWM_ResWorld.md` · 变更记录 `STATUS.md`

---

## 1. 多假设价值链条（路径 A 的答辩词）

推理期 `fused` 输出是 K 个假设的**加权期望**——这常被质疑"多假设被平均化，与单模态基线何异"。本文的立场是：**多假设的价值不在特征输出的期望更准，而在分布以显式形式传导至风险层与决策层**：

1. **分布传导（已实现）**：`outs['mhst']` 输出完整 `(π, σ, ΔB^(k))`，是 Stage 4 风险层与 Stage 5 决策层的显式输入——多模态信息在表示层之后**不坍缩**（对应设计文档 §2 设计原则 2：不确定性显式性）；
2. **最坏假设保留（设计）**：Stage 4 输出 `R_worst = max_k R^(k)`（而非期望），Stage 5 的 Minimax 消费之——"可能冲出人"的假设即使先验概率低，也通过最坏路径进入保守决策；
3. **可解释性**：π 分布可回答"模型认为遮挡区最可能是什么"（空路/静止车/行人），供事后审计与遮挡场景筛选。

**论文表述须严格为**："多假设分布经风险场（Stage 4）与保守规划（Stage 5）传导至决策"，而非"多假设直接生成轨迹"。

**直观类比**：MHST 是"多专家委员会"——对外报告（fused）是综合意见，但各专家意见（π/σ/ΔB）完整存档；安全决策关键时刻采信最保守那个专家（R_worst）。只看综合报告会觉得委员会多余，但安全靠最保守意见。

---

## 2. 实现范围说明（概念公式 vs 实现边界）

设计文档 3.3-5 的"K 条时序序列 rollout"为**概念公式**；当前实现为**单帧 K 假设补丁**（`pred_bev + ΔB^(k)`，一个时刻）。T 步 rollout 属于 Stage 4 的时空演化接口（设计文档 Stage 4 第 4 步，先单帧落地）。论文写作须区分概念与实现，不得声称已实现多步时序 rollout。

---

## 3. 学术性 limitation 与解决状态

### 3.1 边界上下文感受野（✅ 已解决，实现层）

- **问题**：先验网络 `g_prior` 用 `[pred_bev|mask] → 1×1 → 3×3` 卷积聚合邻域，3×3 感受野仅 3 格，而遮挡区可达 10+ 格宽——3.3-2 式的 $\mathcal{N}(x,y)$"边界可见上下文"覆盖不到远边界。
- **解决（已实现）**：改为**多尺度膨胀邻域聚合**——三个 3×3 膨胀卷积（dilation 1/2/4，感受野 3/5/9 格）concat 后 1×1 输出。论文中可将 $\mathcal{N}(x,y)$ 定义为"多尺度膨胀邻域"。

### 3.2 自监督静态偏置（✅ 已设计解决，分工方案）

- **问题**：`L_occ_halluc`（6.2）假设"遮挡区内容在 Δt 内变化不大"，与鬼探头"内容突然出现"的本质相悖——自监督偏向学习静态内容（停靠车辆），动态假设（冲出的人）缺乏监督校准。
- **解决（设计文档 6.2/6.2b）**：分工——
  - **静态内容** → `L_occ_halluc`（混合模型对数似然，Δt 取小）；
  - **动态内容（鬼探头）** → `L_occ_gt`（检测框 GT 栅格化 BEV 占用，零新数据）；
  - **假设分离** → `L_div` 维持 K 假设不坍缩。

### 3.3 σ 校准是训练契约（✅ 已设计解决）

- **问题**：`loss_mhst_sigma` 占位正则仅让 `sigma_net` 参与梯度；σ 的实际语义（不确定性校准）必须由 Stage 6 兑现，否则"Σ_min 防过度自信"的论文承诺不成立。
- **解决（设计文档 6.4）**：`L_uncertainty = |σ − ‖e‖²|`，e 为遮挡暴露误差（与 `L_occ_halluc` 共享同一份暴露数据，零额外成本）；σ 同时在 6.2 混合似然中作带宽被隐式监督、在 6.4 被显式校准为预期误差。

---

## 4. L_occ_halluc 设计讨论记录（为什么棘手、监督谁）

**监督对象**（梯度流向）：K 个 expert（ΔB^k 生成器，假设内容被拉向暴露真值）、先验网络 g_prior（π 给错权重受罚）、共享骨干（间接）。**一句话：它监督的是 MHST 的整个遮挡区条件分布，用暴露后真值这一个信号。**

**三个结构性问题**（讨论结论，方案见 §3.2/§3.3）：

| 问题 | 本质 | 结论 |
|---|---|---|
| 单真值 vs K 假设 | 只有 1 个暴露真值，π 加权下非最优假设梯度被压掉 | 依赖 L_div 维持分离 + 6.2b 动态监督 |
| 时间错位 (t+1 vs t+Δt) | "内容不变"把两个时刻等同，Δt 大/小矛盾 | Δt 取小，只覆盖静态内容 |
| 鬼探头无监督 | 暴露后内容突然出现，t 时刻输入无信息 | 由 6.2b 检测框 GT 显式监督 |

---

## 5. σ 校准两种方式的选择记录

| | A：仅隐式（混合似然自带） | B：显式 L_uncertainty |
|---|---|---|
| σ 语义 | "假设对观测的解释力"（带宽） | "预测误差的平方"（预期误差） |
| 校准机制 | 间接（似然权衡，MLE 式） | 直接（σ = ‖e‖²） |
| 稳定性 | σ 可能被拉向极端（正确假设 σ→0 无限尖） | 有明确目标，不漂移 |
| 论文承诺 | 只靠 Σ_min 兜底，σ 本身不保证反映误差 | **直接兑现**"σ = 预期误差" |

**选定：A + B 双管齐下（方案 A）**——混合似然里 σ 作带宽（分布形态），`L_uncertainty` 校准 σ 语义（预期误差），两者方向一致（误差大的位置 σ 大）。论文表述："似然负责分布形态，校准项负责 σ 语义"。
