# ISSUE: 工程修订与执行记录

> 本文档承载设计修订历史、结构性不变量、训练诊断量、落地顺序与方案决策记录。
> 方法设计（概念与公式）见 `OARWM_ResWorld.md`，本文档不进论文，只服务工程。

---

## 1. 设计修订记录（已实现版本 → 新设计）

### 1.1 修订动机：实验事实

nuScenes 开环评测的非对称结果：

| 指标 | ResWorld 基线 | OARWM（已实现版本） |
|---|---|---|
| L2 Avg | 0.300 | 0.340（早期 0.357） |
| Collision Rate | 参考 0.01–0.14 /s | ~1e-4 |

关键事实：**碰撞率的边际安全性价比已经耗尽，L2 却退化 13–19%**——问题不是"保守换安全"，而是保守得不对：模型在牺牲 L2 规避本不需要规避的东西。

### 1.2 根因定位

| # | 缺陷 | 位置 | 后果 |
|---|------|------|------|
| D1 | 遮挡证据以无监督自由加性扰动写入共享 BEV（`B̃ = B + E_occ ⊙ M`） | Stage 2 | 对轨迹回归是结构化噪声，污染规划特征 |
| D2 | 风险场是内容变化强度的纯超参函数，无安全语义 | Stage 4 | 度量错位：惩罚"预测活跃"而非"危险" |
| D3 | 风险项以 GT-相对 hinge 进规划损失，与模仿项争梯度 | Stage 5 | 风险度量无物理尺度，偏离 GT 即被无差别惩罚 |

### 1.3 修订主线：因果方向反转

```
旧:  遮挡 ──► BEV ──► 风险        （先污染表征，再从被污染表征读风险）
新:  遮挡 ──► 风险 ──► BEV        （先赋予安全语义，再以受控带宽写回表征）
```

### 1.4 逐阶段变更（旧设计 → 新设计；新设计已全部实现并合入主配置）

| 位置 | 旧设计（已替换） | 新设计（当前代码） |
|---|---|---|
| Stage 2 | 自由加性注入 `B̃ = B + E_occ ⊙ M`（键 `use_osz_inject` 已删除） | 风险门控回流 `B̃ = B + g⊙Proj([R_exp.detach(), R_worst.detach()])`，三态 `osz_inject_mode ∈ {off, raw_additive, risk_gated}`（主配置 risk_gated） |
| Stage 3 | MHST（`OcclusionMHSTHead`） | **不变** + 语义锚定头为后续演进（设计文档 §3.4） |
| Stage 4 | 计算式风险场（`build_risk_field` 已删除；超参 `risk_w_sigma`/`risk_gamma` 已删除） | 可学习 `RiskHead`（μ=sigmoid + σ_epis UCB，MC-dropout），`risk_beta` 语义改为概率量纲 UCB 系数 |
| Stage 5 | 三件套相对项（`loss_plan_risk/cvar/info`） | 绝对阈值下界 `L_plan_guard`，`risk_plan_mode='absolute_hinge'`（主配置）；info 已删除、CVaR 仅作消融臂 |
| Stage 6 | 六项损失 | 增加 `L_col`/`L_dyn`/`‖g‖₁` 三项；`L_occ_gt` 目标为动态占用 `occ_dyn` |

### 1.5 已否证的假设（决策记录，勿再回退）

- **"移除 Stage 2 注入可得 +13%"**：该数字出自外部对话记录（`new.md`），作者未见本项目代码、未经隔离实验验证，且与本项目"世界模型应感知遮挡"的出发点冲突。作废。真正要动的是注入的监督与带宽，不是注入本身。
- **"辅助监督回流共享编码器可带来 +2~5% 增益"**：撤回。注入通道开启后，辅助监督回流共享编码器会引入表征漂移，与 D1 同源。辅助监督止步于 RiskHead 与占用头。
- **`L_exp = MSE(μ, e²)`**：监督对象错误。e²（曝光误差）是预测残差，属于分布层校准（L_uncertainty）与内容对准（L_risk_ground）的范畴；用它监督风险均值 μ 会造成概念渗漏。σ_epis 不由 e² 监督，由 dropout 方差统计刻画。
- **深度集成 σ_epis**：弃用。无 bootstrap 的浅副本共享同一输入与初始化，预测坍缩至一致、σ_epis ≈ 0，UCB 通道失效。
- **info 项（L_plan_info）**：移除。开环设定无 re-planning，"主动探测"没有行为出口。
- **轨迹回流（plan → 条件世界模型 rollout → replan）**：暂缓。开环测评无回流出口；训练侧多步特征级 rollout 漂移快，漂移会污染回归路径（与 D1 同构）。闭环阶段（Bench2Drive）再评估动作条件化。
- **e² 消费者 ≤ 2 的制度性约束**：e² 只被 L_uncertainty 与 L_risk_ground 消费，新增损失不得再引入。

---

## 2. 结构性不变量与诊断量

### 2.1 五条结构性不变量

| # | 不变量 | 机制 | 保证什么 |
|---|--------|------|----------|
| I1 | 零点等价 | `g` 零初始化 + warmup 冻结 + expert 零初始化 | 训练初值逐点等价 ResWorld 基线；L2 锚点由初始化保证，不靠关闭功能 |
| I2 | 单向内容 | R_exp/R_worst 进注入前 detach；π/σ/ΔB 进风险场前 detach | 规划损失无法塑造风险场内容（无作弊解） |
| I3 | 带宽受控 | g/Proj 由规划损失训练 + L1 预算 + clamp | 风险信息按需注入，不会被强行灌入 |
| I4 | 辅助监督封闭 | L_col/L_dyn 止步于 RiskHead 与占用头 | 辅助任务不引起共享表征漂移 |
| I5 | UCB 有界 | risk_clamp + softplus + 高斯平滑 + 谱归一化 | 规划器梯度不被尖峰风险场撕裂 |

**实现要点（两条必须写死的初始化约束）**：

1. **零乘死锁**：`Proj` 与 `g` 只能有一个零初始化。若两者同时为 0，∂L/∂g = Proj(·) = 0 且 ∂L/∂Proj ∝ g = 0，门控永远打不开。
2. **梯度饥饿**：即使仅 `g` 为零，∂L/∂g 在 Proj 小增益下极小，叠 L1 后 g 可能长期贴 0。因此 Proj 必须常规增益初始化，L1 权重压至 1e-5~1e-4。

### 2.2 训练诊断量（"日志正常但设计失效"高发区，每轮训练记录）

- **‖g‖₁ 曲线**：warmup 后若恒为 0 → 门控饥饿或风险场对规划无信息量 → 查 Proj 增益 / Stage 4 监督；
- **σ_epis 空间均值与方差**：若 ≈ 0 → UCB 通道失效，R_worst 退化为 R_exp → 查实现；
- **L_plan_guard 激活率**：应远小于 1；恒为 1 说明 R_safe 标定失效（回到 D3 困境）；
- **遮挡子集 L2 与全集 L2 之差**：增益证据应在遮挡子集上度量，全集主要验证"没付出代价"。

---

## 3. 落地顺序与分层回退

### 3.1 落地顺序（步骤 1–4 代码均已实现，训练验证进行中）

1. ✅ `_rasterise_dynamic` 已产出 `S_dyn`（沿朝向 forward margin，默认 2 m）、`occ_dyn` 与接近度特征 `1/(1+d)`；
2. ✅ `RiskHead`（mc_dropout）已实现并接 L_col/L_dyn——**中间检查点（待训练确认）：μ 热力图必须落在动态物前向区域与遮挡出口上**；若仍落在"MHST 预测发散处"，说明安全监督未生效，不得开启注入；
3. ✅ `risk_plan_mode='absolute_hinge'` 已为主配置，EMA 标定 R_safe 已实现——待训练确认 CR 不劣化、guard 激活率 ≪ 1；
4. ✅ `osz_inject_mode='risk_gated'` 已为主配置——待训练监控 ‖g‖₁ 曲线（warmup 后应离开 0；恒 0 则回步骤 2 查监督）；
5. ⬜ 全消融（训练验证通过后执行）。

### 3.2 分层回退

| 层 | 触发条件 | 动作 |
|----|----------|------|
| 通过 | 遮挡子集 L2 ↓ ≥5%，全集 L2 ≤ 0.285，CR ≤ 2e-4 | 采纳 |
| 回退 A | ‖g‖₁ 不开 / 遮挡子集无增益 | 保持 `risk_gated` 关闭，仅保留 Stage 4+5 修正 |
| 回退 B | CR 劣化 | 提高 β 或降低 `risk_safe_quantile` |
| 回退 C | L2 劣于基线 | 查 I1：g 是否真零初始化、Proj 是否常规增益（死锁/饥饿） |

---

## 4. 方案决策记录

- **σ_epis 实现顺序**：M（MC-dropout）起步（十几行、零依赖，先跑通 UCB 链路）→ 若需更强分布外感知，升级为**谱归一化 + RFF 头（简版）**——注：完整 SNGP 的精确后验协方差更新（滑窗/Laplace）在 torch 1.9.1 + cu111 生态手写有坑，简版保留 distance-awareness 即可。**深度集成弃用**（原因见 §1.5）。最终以 β sweep（0 vs 0.5/1/2）判定 σ_epis 通道是否值得保留：若 β 对轨迹行为无影响，砍掉 σ_epis 直接用 μ。
- **显式语义假设**：两步走。第一步语义锚定头（每个假设附加语义占用输出，动态占用监督对齐），验证假设语义可分离性；若可分且下游规划受益，第二步将假设本体迁移至语义占用空间（每假设一张 (H,W,D) 占用配置图，K 获得明确语义，K=2 起步"空 或 存在智能体"）。维度爆炸的顾虑不成立：K 假设在语义占用空间维度为 K×H×W×D（D≈3~5），与 latent 空间 K×H×W×C 同阶；爆炸只来源于逐格笛卡尔积（K^(H×W)），共享假设混合即可避免。
- **轨迹回流**：暂缓，未纳入当前设计。开环测评无回流出口；闭环（Bench2Drive）阶段再评估"动作条件化"（ego 未来位置注入先验网络）作为最小步。
- **L_ctr（条件对比先验）**：默认不开，作消融臂；启用时跟踪构型激活率（遮挡区∧邻接动态物），激活率过低即弃用。